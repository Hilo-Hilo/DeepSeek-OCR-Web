# DeepSeek-OCR-Web Dependencies
# For NVIDIA Blackwell (sm_121) ARM64 systems

# Core ML/AI - PyTorch CUDA 13 (cu130) for Blackwell-family GPUs
# Install separately (recommended):
#   pip install --upgrade \
#     torch==2.10.0+cu130 torchvision==0.25.0+cu130 torchaudio==2.10.0+cu130 \
#     --index-url https://download.pytorch.org/whl/cu130

# Hugging Face
transformers==4.45.0
accelerate>=1.12.0
tokenizers>=0.20.0

# PDF/Image Processing
PyMuPDF>=1.26.0
img2pdf
Pillow>=12.0.0

# Utilities
einops
easydict
addict
numpy>=2.0.0
tqdm>=4.67.0
matplotlib

# Model Download
modelscope>=1.32.0

# Web Backend
fastapi>=0.110.0
uvicorn>=0.30.0
websockets>=12.0
python-dotenv>=1.0.1
python-multipart>=0.0.9
aiofiles>=24.1.0
requests>=2.32.0

# Note: vLLM is installed but NOT used on Blackwell GPUs due to compatibility issues
# The backend uses Hugging Face Transformers with model.infer() instead
